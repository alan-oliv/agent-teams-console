# Artboard 9 inventory — "The run view, rebuilt from what a run actually records"

Screenshots: `9-workflow-9a-top.png`, `9-workflow-9a-9b.png`, `9-workflow-9b-9c.png`, `9-workflow-9c-final.png`.

Prior-round rulings referenced below are read from `docs/CONSOLE-DECISIONS.md` as it stood at
`HEAD` before the working-tree deletion (`git show HEAD:docs/CONSOLE-DECISIONS.md`). That file's
own scoreboard names the artboard 9 replaces **`6a`** — the old workflow-run mock — so its `6a`
rulings apply directly here unless 9 asks for something `6a` didn't.

| Field / value | Verdict | Source or gap |
|---|---|---|
| `OCTO` wordmark | derivable | `Workflow.tsx:84`, `<Bar wordmark="OCTO">` — already built |
| `workflow` pill | derivable | `ConsoleMode = 'team' \| 'workflow'` (`domain.ts:402`); `modeOf()` (`server/workflow.ts:255`) selects it. Rendering as a pill next to the picker is a layout question for task 3/4, not a data gap |
| Breadcrumb ("where does the evide…" / "find flaky tests and…") | partial | This is the run's own name/description truncated. `WorkflowRun.name` (from `meta.name`) and `.description` (from `snapshot.summary`) exist but only on a **finished** run — both are explicitly absent on a live run (`domain.ts:539` "Absent on a live run: the name reaches disk in the snapshot"). 9a/9b are live and still show a breadcrumb in the design, which the data can't back until termination |
| Run picker (name + `wf_<id>`) | derivable | `RunSelect` (`chrome/RunSelect.tsx`) already renders `runLabel(run)` (`run.name ?? 'unnamed run'`) and `run.runId` |
| Second "session" picker (9b: "two pickers in the bar — session, then run") | partial | Code has `TeamSelect` (session/team switcher) + `RunSelect` (run switcher) side by side in `Workflow.tsx:87-100` — two pickers already exist. Whether they read as "session, then run" the way 9b frames it (a session accumulating runs, `runId` disambiguating two runs of one script) is a phrasing/grouping question, not a missing source — `TeamState.workflows` is already the session's list of runs |
| Tabs `run \| agents \| script \| journal \| usage` | derivable | `WORKFLOW_VIEW_IDS` (`Workflow.tsx:16`) — prior ruling: canvas scoreboard row `6a "five pills run·agents·script·journal·usage"` → **already correct** |
| Live corner stats: `4.9M ctx · 583 tools · 15m46s` | derivable | `run.totalTokens`/`run.totalToolCalls` (finished) or `liveCounts()` (live); `formatTokens`/`formatElapsed` in `format.ts`. On a **live** run only `started`/`returned` counts exist today (`runTotalsText`, `workflow-grid.ts:130`) — live token/tool-call totals are not on disk until the snapshot lands (`domain.ts:560-561` `totalTokens`/`totalToolCalls` "absent while running"), so the *live* ctx/tools figures 9a shows are themselves partial (see the `usage` row below, which is the actual live source) |
| Settings/gear icon | derivable | `<Bar>` already renders one (appearance/theme control) in every mode |
| `✓ returned 14:21:08` swap for the live pulse (9c) | derivable | `run.status` ('completed'/'killed'/'failed'/'running') and `run.durationMs`/`run.startedAt` are on the finished snapshot; `RUN_STATUS_COLOR` (`RunSelect.tsx`) already colours by status. The literal `✓ returned <time>` chip next to the corner stats isn't drawn today — a rendering gap, not a data gap |
| Sub-header: "5 phases · 36 agents · grouped by dispatch instant" | derivable | `run.phases.length`, `run.agents.length` (or `agentCount`); "grouped by dispatch instant" is prose describing `clustersOf()`'s `queuedAt` grouping (`workflow-grid.ts:184-204`), which already exists |
| "no item grid — labels do not resolve to one set of work items shared across the phases" | derivable | Verbatim match — `wf-no-grid` in `WorkflowRun.tsx:361-367`, driven by `gridCooperates()` |
| "7 work items shared across every phase · each row carries the item's trail" (9b) | partial | The grid/trail concept exists (`workflowGrid()`, `itemKeyOf()`, `gridCooperates()`) but today it's an **alternate layout the operator toggles to** (`BY PHASE` / `ITEM GRID` tabs, `wf-layout-*`), not "folded into the list" the way 9b draws it (phase-grouped rows that also carry a cross-phase trail icon). The underlying join is derivable; the fused presentation is a layout gap for task 3/4 |
| Phase title + detail (e.g. **Fetch** — "URL-dedup, fetch top sources, extract falsifiable claims") | derivable | `WorkflowPhase.title`/`.detail` (`domain.ts:424-428`), rendered by `PhaseHead` (`WorkflowRun.tsx:246-272`) |
| Phase status-summary counts (e.g. "25 returned 1 null", "2 running 1 returned") | derivable | `phaseTally()` (`workflow-grid.ts:227-234`) — same tally order and wording (`returned`/`running`/`cached`/`failed`/`blocked`/`returned null`/`queued`) already implemented |
| Per-item rows (URL / spec path) with status word | derivable | `AgentRow` (`WorkflowRun.tsx:274-302`), `WorkflowAgent.label`/`.agentId`, `GLYPH`/`GLYPH_COLOR` per state |
| Per-item short id (`a696d62d`, `spec_101` etc.) | derivable | `WorkflowAgent.agentId` (`a` + 16 hex, `domain.ts:438`) — the design's shortened hex is a formatting choice over the same field |
| Dispatch-group header "N dispatched together" | derivable | `DispatchCluster.together`, rendered at `WorkflowRun.tsx:436-443` — wording matches |
| Dispatch-group **timestamp** ("· 14:04:02") | partial | The count is rendered; the clock time next to it is not. The data exists (`WorkflowAgent.queuedAt`, epoch ms, is the cluster key itself — `workflow-grid.ts:189`) but nothing formats/renders it today — an easy add, not a source gap |
| "+N more, all returned" collapse | partial | No collapsing exists today — every clustered agent is listed in full (`cluster.agents.map(...)`, `WorkflowRun.tsx:444`). Data to compute "N more, all returned" is present (cluster length minus shown, and a check that every remaining agent is `done`); the truncation behavior itself isn't built |
| Trail icon on a shared item's later-phase row (9b, pointing back to `Fix`) | absent | Nothing in the grid/phase code renders a "this item also appeared in phase X" affordance on the by-phase list — `workflowGrid()` can answer "which phases did this item's key appear in" but no UI consumes that for a trail glyph outside the separate ITEM GRID layout |
| Legend (`✓ returned`, `●running`, `↻cached`, `⊘null`, `✗failed`) | derivable, glyphs differ | `Legend`/`LEGEND`/`GLYPH` exist (`WorkflowRun.tsx:27-45,179-213`) and cover the same five ideas plus `block`. Glyph shapes differ: app uses `⤿` for cache and `∅` for null where the mock shows `↻` and `⊘` — a cosmetic mismatch for task 3/4, not a data gap |
| Barrier-kind caption ("a rule marks agents that share one dispatch timestamp — the barrier kind is a property o[f the phase]") | absent | `WorkflowPhase` has "deliberately no `kind` and no barrier flag" (`domain.ts:419-421`) — a barrier belongs to an individual `parallel()`/`pipeline()` call inside the script text. `run.script` is stripped from every wire frame (`leanRun`, `server/workflow.ts:259-269`) specifically because nothing reads it; `WorkflowScript.tsx` can fetch the full source on demand (`/api/workflow/:runId/script`) but nothing parses `parallel()`/`pipeline()` calls out of it into a "kind" today |
| Right rail — LIVE header, `4.9M ctx` / `583 tools` / `15m46s` | partial | Same figures as the top-corner stats; on a live run these totals aren't in `WorkflowRun` at all — `wf-totals` (`WorkflowRun.tsx:494-503`) shows only `started`/`returned` counts for a live run today. The *actual* live token source is `WorkflowUsage.split`/`.burn`, read from agent transcripts as they're appended (`workflow-usage.ts`, `domain.ts:502-519`) — derivable from `run.usage`, just not what the current `RUN TOTALS` panel draws |
| Right rail — FINAL header, `5.1M ctx` / `601 tools` / `19m 02s`, frozen | derivable | `run.totalTokens`, `run.totalToolCalls`, `run.durationMs` — all present on a finished snapshot and already rendered by `wf-totals`'s non-live branch |
| "the run's own spend · a run has no budget to measure it against" / "what the run spent, frozen at return" | derivable | Near-verbatim match to the existing `budget` row's note (`WorkflowRun.tsx:509-516`: "no budget on disk · this is the run's own spend, not the session's"). Prior ruling: canvas scoreboard `6a "budget bar · budget text"` → **blocked**, "phases and budget reach disk only in the terminal snapshot" / `domain.ts:526-528` "`budget` is absent by design... exists nowhere on disk" |
| AGENTS panel: `36 of 1000 lifetime cap` + bar | partial | The count (`run.agentCount ?? run.agents.length`, live: `live.started`) is derivable and already rendered (`wf-limits`, `WorkflowRun.tsx:534-538`, "lifetime cap" label). **`1000` is a hardcoded literal in the component, not a value read from disk** — there is no `WORKFLOW_AGENT_LIFETIME_CAP` field anywhere in `domain.ts`/`workflow.ts`, so it's a UI constant rather than a per-run figure. The usage **bar** (visual fill) isn't drawn today — only the text row is |
| AGENTS breakdown (returned/running/cached/null/failed counts) | derivable | Trivial `run.agents.filter(a => a.state === X).length` over `WorkflowAgentState` — same states `phaseTally`/`liveCounts` already use elsewhere; just not aggregated into this specific panel today |
| CONCURRENCY: `min(16, CPUs − 2) agents at once` | derivable (formula text only) | Exact text already exists — `WorkflowRun.tsx:529-533`, `note="min(16, CPUs − 2) agents at once · the slot count itself is not recorded — only the formula is known"` |
| CONCURRENCY resolved slot count / "9 of 10 slots" style live gauge | absent | Explicitly modeled as absent: "The cap is resolved from the HOST's cpu count at launch and never written to the snapshot… the figure is named as missing rather than substituted" (`WorkflowRun.tsx:525-533`). No field on `WorkflowRun`/`WorkflowAgent` carries a resolved concurrency number or a live "N of M slots" count |
| NARRATION log lines (timestamped free text) | derivable | `WorkflowRun.logs: string[]` (`domain.ts:542`, `log()` narration in order), already rendered verbatim by the `wf-log` panel (`WorkflowRun.tsx:544-556`). Design shows each line **prefixed with a clock time** ("14:02:39 decomposed into 6 angles") — `logs` is `string[]` with no separate timestamp field, so a leading time in the design's lines is either baked into the log string by the script itself or would need a timestamp alongside each entry, which isn't recorded (`domain.ts:542` carries no per-line `ts`) |
| "you are not in the loop — a workflow opts in at launch and reports when it lands" | derivable | Near-verbatim — `wf-not-in-loop` (`WorkflowRun.tsx:559-564`): "you are not in the loop — a workflow opts in at launch and reports at the end. Nothing here steers it." Also the substance of ruling 35 (run controls refused on four independent channels) |
| **Returned** box (9c): header "✓ Returned  returned 14:21:08 · 19m 02s" | derivable | `run.status === 'completed'` + `run.durationMs`/formatted return time from `run.startedAt + durationMs`. No component renders this box today — see next rows for why it's still classified derivable rather than absent |
| **[copy return]** button | partial | Requires `run.result` as a copyable string. `WorkflowRun.result` (`domain.ts:563`, `snapshot.result`) is on disk and parsed (`workflow.ts:191`) but can be `null`/an arbitrary JSON value once stringified (`resultText()`, `server/workflow.ts:25-33`) — copyable, but not guaranteed to be prose. No UI reads `run.result` anywhere today (only per-agent `WorkflowAgent.result` is shown, in the Journal tab) |
| **[open in journal]** button | derivable | The Journal tab/view already exists (`WorkflowJournal.tsx`) and is one `view` switch away (`Workflow.tsx:168`); wiring a button to jump there is plumbing, not a missing source |
| Summary line "61 falsifiable claims from 24 sources · 4 contradictions left unresolved" | absent | Nothing on `WorkflowRun` counts claims, sources, or contradictions — this is domain-specific structure inside `run.result`'s *content*, which the console has never parsed. `result` is opaque prose/JSON (`resultText()`), not a shape with countable claims |
| 3 paragraphs of brief prose (the opening of the return value) | partial | The full text is `run.result` when it's a string; showing "the opening" (a prefix) is a display slice of an already-derivable value. If `result` is a JSON-stringified object (schema'd agent return, per `resultText()`'s own comment) it may not read as prose at all — partial on that branch |
| "full return — 2,140 words, 61 citations — in the journal as the run's final entry" | absent | Word count is computable from `run.result` text (`result.split(/\s+/).length`), so that half is derivable; **citation count is not** — nothing parses citations/URLs out of the return text or tracks a citation count anywhere in `domain.ts` |
| Collapsed phase headers in 9c (no item rows, just final counts) | derivable | Same `PhaseHead`/`phaseTally` already used for the live phase list — collapsing to header-only on `run.live === false` (or on a click-to-reopen toggle) is a display-state question over already-derivable data. "Phases collapse to their headers (click to reopen)" is new interaction, not new data |
| "re-invoke with this run id to resume from cache" | derivable | `run.runId` exists; whether re-invocation is actually wired up is a separate question from whether the *id* is derivable (it is) — resume affordances are discussed in `workflow-resume.ts` already in this codebase |
| Design "try next" callouts beneath 9a/9c (e.g. "open an agent row into the 5b drawer…") | out of scope | These are notes-to-self on the design canvas about *further* artboards to build (5b's drawer), not content the artboard itself draws — excluded from this inventory |

## Tally

- **derivable:** 25
- **partial:** 9
- **absent:** 5

(40 rows total; the "try next" callouts row is out-of-scope/informational and not counted in the tally above.)
