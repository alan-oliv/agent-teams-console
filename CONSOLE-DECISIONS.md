# Console decisions

## What outranks what

1. **The canvas** — `design_handoff_agent_teams_console/Octo Agent Console - Canvas.dc.html`.
   The MANIFEST says it *is* the handoff, so it is the top authority on anything visible:
   layout, density, what furniture a pane carries, what the bar says.
   **It cannot be read by grepping.** Serve the directory and look at it:
   `cd <handoff-dir> && python3 -m http.server 8099`, or open the `standalone.html` copy.
2. **The prose docs** — `README.md`, `CHANGELOG.md`, `MESSAGING.md`, `USAGE-DASHBOARD.md`.
   They govern where the canvas is silent, and explain intent behind what it draws. Where a
   prose doc and the canvas disagree about a *picture*, the canvas wins.
3. **This file**, for the roughly thirty places the bundle contradicts *itself*.
4. **The existing code**, last — "the app already does X" is evidence, never an argument.

A row here settles a conflict between sources. It does not license a departure from the canvas:
those are marked `deviation` below and each one owes a reason.

**How the reconciliations were decided**

1. The CHANGELOG's top entry (*Reconciled with the console at 0.6.5*) is the newest prose voice.
2. The canvas governs where prose is silent — its build target is turn `4a` for team mode,
   `6a` for workflow, `8a`/`8b` for sub-agents, not the earlier reference frames.
3. A measured argument (contrast, a width someone actually measured) beats an aesthetic one.
4. Standing rule 1 still holds: a control that names no runtime call does not ship.

Every ruling below has been applied to the console. Where a row describes what "the app" does,
that is the state at the time of the ruling — kept because it is the evidence, not a status.
Numbers are permanent: 39 code comments cite them, so rows are never renumbered or removed.

## Kinds

| Kind | Meaning | What it owes |
|---|---|---|
| `reconcile` | The bundle contradicts itself; we picked a side. | Nothing — the sources are in the row. |
| `blocked` | The design is right, the data does not exist. | The missing field, named. Reopen when it lands. |
| `gap-fill` | The design is silent; we invented something. | Nothing, but it is not design-backed — say so. |
| `deviation` | **We chose differently from the canvas.** | The artboard, looked at, and what it gives up. |

## The rulings

| # | Kind | Conflict | Sources | Ruling | Why |
|---|---|---|---|---|---|
| 1 | `reconcile` | Quiet-register colour and size | CHANGELOG `neutral-600` @ 10px (measured 2.69–2.80:1 against `-700`) · README `neutral-700` @ 10.5px · app split | **`--color-neutral-600` at 10px.** Scope in the note below. | Measured: `-700` on `--color-bg` is 2.69:1, `-600` is 4.09:1. Contrast beats taste. |
| 2 | `reconcile` | What the `!` context warning reads | app 0.75 × `compactAt` (≈63% displayed on a 200k window) · README "past the threshold (default 75%)" · prototype `p = tok/200000; p >= warnAt/100` | **The displayed percent: `tokens / contextLimit >= 0.75`.** Formula in the note below. | README and the reference build agree, and a configurable "75%" that lights at 63% on screen is a setting the operator cannot verify. |
| 3 | `reconcile` | Status-bar metric labels | README `tasks 3/11` + `6 context windows` · CHANGELOG `3/11 tasks` + `4 ctx` · prototype `4a` `3/11 tasks` + `4 ctx` · app splits the bullet (`tasks 3/11` + `4 windows`) | **`3/11 tasks` and `4 ctx`.** | Newest voice and the built screen agree, and the shortening was paid for by a measured overflow — the bar passed 1180px and silently doubled in height. |
| 4 | `deviation` | Transcript line gap | README 6 / 7 / 4–5px · CHANGELOG 10 / 11 / 7–8px · prototype density-driven | **Density-driven.** `compact 5 / default 10 / roomy 16` in wall and rail panes; condensed panes take `max(3, density − 3)` → 2 / 7 / 13. | Both fixed sets are snapshots of one density setting, and `line density` is a config control that must actually change the render. App is already right. |
| 5 | `reconcile` | Glyph marker colour | README `--color-accent-600` · CHANGELOG + prototype + app `--color-accent-500` | **`--color-accent-500`.** | The CHANGELOG unified markers on `-500`; README's `-600` is the pre-unification value it never went back and fixed. App is already right. |
| 6 | `reconcile` | Transcript line text colour | README says `--color-neutral-300` and `--color-neutral-500` in the same paragraph | **`--color-neutral-300`.** | `-500` is the flat base colour the per-line opacity ladder replaced — "the fade does the ranking, not a dim base colour". The `-500` half of the sentence is the leftover. App is already right. |
| 7a | `reconcile` | Chat bubble grounds | README `--color-bg` / `neutral-900` edge vs `accent-900` / `accent-700` edge · prototype `neutral-900` / `neutral-800` edge vs `accent-700` / `accent-600` edge · app uses the prototype pair in the everyone room and the README pair in the pair thread | **One pair, both rooms.** Left speaker: `--color-neutral-900` fill, `--color-neutral-800` edge, `--color-neutral-200` text. Right speaker (the operator, or the second participant): `--color-accent-700` fill, `--color-accent-600` edge, `--color-text`. | `accent-900` with an inset `accent-500` is the *selected row* tint everywhere else in the console, so a bubble drawn on it reads as a selection. The prototype pair also measures 5.6:1 for its text. Same reasoning as `max-width: 64%` — one constant, consumed by both rooms, so they cannot drift. |
| 7b | `reconcile` | Everyone-room label and subtitle | README "everyone" / "every message, one room" · CHANGELOG + prototype + app `all messages` / `every inbox, merged` | **`all messages` / `every inbox, merged`.** | There is no group inbox; the room is a view over every inbox, and the subtitle should say so rather than imply a room that exists. App is already right. |
| 7c | `reconcile` | Recipient label under a room line | README `to <agent>` · prototype `→ <name>`, `to everyone` for a broadcast | **`→ <name>`, and `to everyone` only when the fold reached every other member.** | `→` is the routing arrow the mailbox meta line already uses; "everyone" is not a recipient an arrow can point at, because no such inbox exists. The mixed form is the point. App is already right. |
| 8 | `reconcile` | Composer hint | README `⌘⏎` · MESSAGING §5 + CHANGELOG + prototype `⏎` | **`⏎`.** Enter sends, ⇧⏎ newlines, ⌘⏎ still sends but is not named. | Naming the one key that used to do nothing was most of the old trap. App is already right — recolour the glyph per ruling 1. |
| 9 | `reconcile` | JSON boolean token | CHANGELOG diff-viewer entry `--json-bool` · README standing rule 2 + CHANGELOG 0.6.5 + app `--json-boolean` | **`--json-boolean`.** | The two newest voices agree and the app ships it; the `--json-bool` spelling predates both. The prototype's `--json-bool` is the same stale spelling. App is already right. |
| 10 | `reconcile` | Task table column widths | README `STATE` 92px / `DEPENDS ON` 88px · CHANGELOG 118 / 76 (arrives with the stepper) · prototype `4a` and `3b` draw 118 / 76 | **`STATE` 118px, `DEPENDS ON` 76px**, `MODEL` 60px between `STATE` and `OWNER`, `DESCRIPTION` flex. | The four-cell stepper needs the width and the ids in `DEPENDS ON` do not; the prototype already draws the post-stepper widths. README's 92/88 predates the stepper. |
| 11 | `reconcile` | The `·` cell state word | README (Screen 3) "queued for a slot" · CHANGELOG (turn-6 entry) "waiting" · prototype legend omits `·` entirely | **`queued`.** | The top CHANGELOG entry explicitly hands workflow mode to README's Screen 3 rewrite, which makes README the newest voice here; the prototype is silent, so prose decides. |
| 12 | `reconcile` | `respawn` on the failure card | Standing rule 1 struck it · README Screen 1 still specifies it · app ships it, backed by `POST /api/agents/{name}/respawn` → a lead-inbox write (`src/server/http.ts:374`) | **Kept.** The card names what the call does — it asks the lead to respawn the teammate — and must never imply the console restarts a process. | Standing rule 1 removes controls with *no* call. This one has one. Rule 1 satisfied is rule 1 obeyed, not evaded. |
| 13 | `reconcile` | `experimental` pill | README Screen 1 keeps it · CHANGELOG drops it from the 4a bar · prototype has it in `3a` only | **Dropped.** | It was traded for branch + diffstat when the bar ran out of room. README's Screen 1 documents `3a`, the earlier reference frame — it is not wrong, it is not about the screen being built. App is already right. |
| 14 | `reconcile` | Session picker: trigger content, menu width | README trigger = session id + goal, menu 432px · CHANGELOG config-panel entry: the id came out and the goal capped at 146px · older CHANGELOG entry + app: 432px · prototype `4a`: goal alone at 146px, menu **520px** | **Trigger: the goal alone, capped at 146px, plus the optional in-world team chip and the caret — no session id. Menu: 520px.** | The gear cost the bar ~30px and the id is what paid for it; app already matches. The width follows the same entry: the reconcile turned each row into two lines, and 432px predates that. Measured below. |
| 15 | `reconcile` | Marking every non-lead wall column `read-only` | README:118 (the One-composer rewrite) "Every other column is marked `read-only`" · the prototype draws no such marking · standing rule 4 says do not write what the prototype does not do | **Kept.** Every non-lead column and the rail's non-lead pane carry it. | Standing rule 4 guards against prose the reference build **contradicts**, not prose it **predates**. The prototype was drawn before the single-composer model existed, so its silence here is an absence, not a disagreement — and the newest voice governs. Without the marking, a column with no composer is indistinguishable from one whose composer failed to render. (Ruled by the team lead during the one-composer close-out; recorded so the next reader of rule 4 does not reopen it.) |
| 16 | `reconcile` | The read receipt under a bubble (`read at turn 9`) | README:105 names `var(--color-neutral-700)` for it · ruling 1 says that register is neutral-600 · prototype: `stateCol: unread ? '#d99e5c' : 'var(--color-neutral-600)'` | **`--color-neutral-600` when read.** The unread branch keeps `--warn` — it is a semantic colour and stays. | Ruling 1 overrules the README here, and the prototype agrees with the ruling rather than the prose: it draws the settled receipt at neutral-600 and only the unread one at the attention colour. Recorded because README:105 states `-700` outright, so a reader finding this in the code needs to know it was overruled and not simply missed. |
| 17 | `blocked` | `PR` beside the diffstat | README Screen 1 bar `PR + diffstat` · README data model `PR/diffstat` per team · no source anywhere in the console | **Diffstat shipped, PR refused.** The picker row carries `+N −M` for what is uncommitted against `HEAD`, titled with that reading. Nothing names a PR. | Standing rule 3. `gh` is an optional install; a forge API needs a token, a network call and a per-forge client, which a local read-only console makes none of. `.git/config` yields the remote URL and the tracking branch — the repo, never the pull request — and `refs/remotes/*/pull/*` exists only if someone fetched that refspec by hand. A field with no source is blank for every operator, and nobody reports a blank. Diffstat is scoped to uncommitted work for the same rule: branch-against-base needs a base branch, which means guessing `main` or reading an `origin/HEAD` most clones never set. |
| 18 | `gap-fill` | What the workflow bar sheds first when it runs out of room | README:64 gives the TEAM bar a drop order (`diffstat first, then combine elapsed and spend, then shed the token figure`) · the design never states one for the workflow bar (`6a`) · the app shed nothing there at all, so the run's figures ran off the frame under 1094px | **`totals`, then `elapsed`, then `taskId`** — lower survives longer, as on the team bar. The mechanism moved into the shared `Bar` so both modes shed; each keeps its own order. | The team bar's precedent is that identity outlives figures (`branch` survives every readout), and the run's identity here is the task id: it names the piece of work the run belongs to and is the one metric not re-derivable from the view below. Elapsed sits where the team bar puts its elapsed-and-spend chip. The totals go first because the agents view carries them in full. Measured, not assumed: with all three shed the bar's remaining furniture still costs 741.48px, which is why the run trigger's cap is `min(236px, 26vw)` rather than a flat 236px — see ruling 19. |
| 19 | `gap-fill` | The run-picker trigger's width bound | The team trigger pins a flat `146px`, derived from `session-` + 8 hex · the run trigger shipped unbounded, because run names have no shape to derive from · the visual smoke-check then measured a 65-char run name at 610px, 87% of a 700px bar, with every view pill off the frame | **`min(236px, 26vw)`**, with the name ellipsised and the runId and caret never shrunk. | Two terms because one number cannot serve both ends: `236px` is what the 1180px design width can spare and keeps a typical name whole; `26vw` is the narrow end, where the bar's un-sheddable rest (padding, wordmark, the 146px team trigger, four pills, spacer, and the gear, which is chrome and never sheds) costs 741.48px and leaves the trigger at most 27.79% of a 700px frame. A max-width and never a flex-shrink — every child of this bar is `flex: none`, and one that could shrink would wrap and double its height. The identity survives the squeeze whole because a half-drawn runId identifies nothing. |
| 20 | `reconcile` | Cast overflow: spares first, or real names first | `movie-themes.json` `overflowKeepsRealNames`: "leaves the extras under their real names. Draw from `spare` **only** if you need more named agents; never invent characters" · README:135 renders the same rule without the *only* — "draw from `spare` if you need more" · CHANGELOG:69 lists spare characters as first-class content of the database, beside the role mapping · the prototype casts nobody · app draws spares in roster order, then falls back to real names (`src/shared/cast.ts:95-98`) | **Spares first, then real names. App is already right.** An agent no free role slot matched takes the next unused `spare` in join order; when the spares run out, every remaining agent keeps its real name. A **vacant role slot is never filled** by an unmatched agent — it stays vacant. | Measured, and it is not close: across the eight teams in `~/.claude/teams` on this machine — 20 members, 8 leads — **zero** non-lead agents match a role slot, because the real types are `general-purpose`, `claude` or unset and none of the five patterns touch them. Real-names-first would therefore rename the lead and nobody else on every team this console has ever run, which is a themed console with no theme. On the prose: sentence 2's condition ("you need more named agents") is the same condition as sentence 1's, so the exception governs the case, and the floor sentence 1 guarantees is kept as the exhaustion path — extras really do end under their real names, once there are more of them than the film has spares. `never invent characters` is the clause with teeth and is untouched. And spares, not vacant seats, are what an unread role gets because **`spare` is the film's role-free pool**: `portraitFor` hashes a face for an agent it cannot place, so seating one in `security` would put "Arthur" on an agent drawn with a random portrait — a name promising a role the console never read. The join-order seeding of the cast (`src/web/App.tsx:213-225`) stays load-bearing under this ruling: spares are dealt positionally, so the roster order they are dealt from must be the append-only one. |
| 21 | `blocked` | How much of rev 3c's workflow-usage spec gets built, when every dollar in it has no source | `USAGE-DASHBOARD.md` *Workflow mode*: five tiles, a phase Gantt, concurrency, burn-vs-projection, a per-agent scatter, relaunch economics, a per-phase agent table · `USAGE-STATE.md` §4 and `WorkflowUsage.tsx:7-30`: `workflowProgress[].tokens` is final context occupancy, so no dollar figure has a source · the app drew none of the four panels rather than drawing their halves | **Split every panel into its geometry and its money. Build the geometry now; keep the money absent.** In scope for the workflow usage view: the phase Gantt as a real track (bar positioned by percentage over the run's span, status dot, agent count, elapsed — no tokens or cost column), the per-agent scatter (**y = final context, labelled as such; x = the agent's own elapsed time; radius by `toolCalls` per `USAGE-STATE.md` §6, captioned as tool calls, with a missing count drawing the minimum radius and never a zero; status by fill with a hollow ring for failed**; filter chips carrying counts), and the per-phase agent table that a phase-row click scopes (index · label · model · status · time · final context, em-dashes for cache hit and cost). Out of scope and staying out: every dollar, the `cap 16` line, the `started/planned` denominator, `N agents queued`. The *"why there are no costs here"* panel is **re-worded, not extended**: the accurate sentence is no longer "no source". | A panel is not atomic, and treating it as one is what lost the geometry. The Gantt's bars, the scatter's points and the phase table answer *where did this run's time go and which agent held the most context* — questions with a complete source in `workflowProgress[]` — and none of them needed the cost column that got the whole panel dropped. Re-measured on 8 runs / 76 agents before ruling: the snapshot understates real token traffic by **15×–75×** per run (6.19M recorded against 280.30M actual; cache reads are 94.8% of it), so the money genuinely cannot be drawn. But the same re-measure found the four-class usage sitting in `subagents/workflows/wf_<runId>/agent-*.jsonl` with a per-line `timestamp` and `model`, appended **live** — so the figures are un-ingested rather than unrecorded, and "no source" is a sentence the console can no longer honestly print. Full panel-by-panel table, and the one scope decision that would unlock the money half, in `CONSOLE-NOTES.md` §24. **Prior art:** `USAGE-STATE.md` §6 already ruled most of this set. Its *outcomes* are upheld in full — nothing it refused becomes drawable today — but its *reason* is overturned on four rows (run/projected/per-phase/per-agent cost, the burn-vs-projection panel, and the prefix-cache tile): each says "no source", and the source exists. Its scatter row **overrules mine** — radius by `toolCalls`, not the fixed radius I first ruled — because a real per-agent measure in the radius beats an empty channel, and substitution over omission is this console's own established pattern. Its `started/planned` and 1.5M rows are confirmed, with evidence added. |
| 22 | `blocked` | The Large-workflow banner's second trigger: 1.5M projected tokens | `USAGE-DASHBOARD.md`: the banner fires past `agentWarnThreshold` scheduled agents **or** 1.5M projected tokens · app fires on the agent count alone (`bannerFires`) | **Refused, and it stays refused even after the workflow transcripts are ingested** — the constant must be re-derived against whatever quantity is settled on, never carried across. The agent-count trigger is the whole of the rule. | Measured on the 8 runs on this machine, and it fails in both directions at once: against the tokens the console can see today, **0 of 8** runs reach 1.5M (the largest is 1.42M), so the line is dead; against the tokens actually spent, **8 of 8** reach it — the smallest run passes it by 6× and the largest by 71× — so the line is permanently tripped. A threshold that is either never met or always met depending on which number feeds it is not measuring anything. Ingesting the transcripts fixes the missing quantity but not this constant. |
| 23 | `deviation` | How a solo session enters the console, when the picker refuses lead-only rows outright | rev 5 README: *"`trace` — one new view, **in solo sessions**"*, and the CHANGELOG: subagents get *"no third mode"* · `CONSOLE-NOTES.md` §12 and the app: a lead-only row is dropped, and revealed it is inert — `no team · not selectable` · `isEmptySession` (`useHiddenSessions.ts:47`) already carries **one** exception, for a workflow run | **No third mode, and no carve-out either: widen the exception `isEmptySession` already has.** The bar stops being *two or more members* and becomes *is there anything in here to look at* — `members >= 2` **or** a workflow run **or** a subagent tree. A solo session with subagents becomes an ordinary listed, selectable row whose activity cell says what is in it (`solo · 13 subagents`); ~~a bare window keeps §12's treatment exactly — dropped, revealable, inert, `no team · not selectable`.~~ **Superseded: bare windows list and switch like everything else.** The carve-out survived only because a bare window had nowhere to go; it now draws its own stream, so the filter and its `reveal` escape hatch are both gone. The ✕ stays — that is an operator action, not a filter. The header goes back to the design's **`SESSIONS ON THIS MACHINE · N`**, counting every listed row. | §12 is not being reversed, its premise expired. It refused rows with *nothing behind them* and said so — *"switching to one would put it straight back on a session with no team, which is the thing being explained"* — and that reasoning is untouched for a bare window. The workflow clause is the precedent and it was ruled for **this exact argument**: a workflow's agents never enter `members[]`, so a session running one "has a roster of one and is somewhere to go all the same". A Task subagent never enters `members[]` either, by the same deliberate exclusion (`hooks.ts:192-194`). Measured on this machine: of the 8 sessions the picker enumerates, 2 are teams and 6 are refused — and **2 of those 6 have subagent trees, one of them 13 transcripts deep**, more agent activity than either team it does show. On the header word: §12 changed `SESSIONS` → `TEAMS` to stop four windows reading as four teams, which was compensating for a filter that listed windows. With the filter fixed the compensation is what is now wrong — the list holds teams, workflow sessions and solo sessions, and only `SESSIONS` is true of all three. Bare windows still never list, so the conflation §12 killed cannot come back. |
| 24 | `deviation` | What a solo session's view switcher contains, when the design names a `stream` view the console does not have | rev 5 README: `stream · trace · usage` · the console's `ViewId` is `wall · overview · comms · tasks · rail · grid · usage`, with no `stream` | ~~**`stream · trace · tasks · usage`, and `stream` is not a new view.**~~ **Superseded: `stream · trace`, two pills.** On a roster of one the **`wall` renders as a single column, which already is the parent's stream** — so the view id, the route and the component are unchanged and only the switcher's LABEL reads `stream` when the roster is one. `trace` is the one genuinely new view. `tasks` is added to the design's list. `overview`, `comms`, `rail` and `grid` are not offered on a solo session. | One rule, not four exceptions: **a view is offered when its subject exists.** The four that drop are all compare-the-teammates views with nothing to compare — `comms` has no pairs at all, since a second inbox is what a pair thread is made of, and the other three would each draw one pane. Building a `stream` view would be building a second `wall` that differs only in how many columns it has, which is a prop, not a view. On adding `tasks`: measured, **9 of the 10 non-empty task lists on this machine belong to sessions that are not teams** — the task tools are session-scoped, not team-scoped, so leaving `tasks` out would mean an operator has to form a team to read their own list. The design's three-item switcher is not wrong about `trace`; it just never considered a task list on a session with no team. |
| 25 | `gap-fill` | How a one-agent team's lead stops reading `working` forever, without departing every replayed log | `CONSOLE-NOTES.md` §17 (known broken since 0.6.5): staleness is `latestActivity − act`, and on a one-agent team that agent **is** `latestActivity`, so the difference is always 0 · the same note records why the obvious fix is wrong — measuring against `Date.now()` departs every fixture-backed agent, because a fixture's records are days old while the events carrying them are new | **Measure against the wall clock, but only on a log that proves it is on the wall clock** — `isWallClockLog(mtimeMs, lastRecordTs)`: the transcript file's own mtime against the newest timestamp inside it, within `LOG_CLOCK_SKEW_MS` (5 min). A log appended live has both clocks together, and they stay together as it goes quiet — ten hours later the file is ten hours old and so is its last record. A replay is written **now** and describes days ago. Absent mtime (an event a test built, never a file) is never wall-clock. And when the wall-clock branch fires the agent becomes **`idle`, not `departed`** — the existing relative branch keeps `departed` and runs first, so multi-agent behaviour is untouched. | The two branches fire on different evidence and so may not reach the same word. The relative rule fires when *other agents are demonstrably active*, and that contrast is real evidence the silent one died — which is what its `departed` was written for. The wall-clock rule fires on **silence alone**, and silence is evidence that nothing is happening, not that anything is gone. `idle` says exactly that much; `departed` claims more than the console can see, and on a one-agent team it draws the only agent on screen at dormant opacity, which reads as a dead console rather than an operator who stepped away. On the mechanism: the cheaper signals were tried and both fail. `StoredEvent.ts` (the server's clock at append) cannot work, because a console STARTING on an already-silent session ingests old records *now* and looks exactly like a fixture. "Has the file appended since we started watching" cannot work either — a session quiet for ten hours and a replay both sit perfectly still, so it fails on precisely the case being judged. The file's own mtime is the one signal that is a property of the log rather than of when we happened to read it. |
| 26 | `reconcile` | Whether the fix above lets the picker's `live` mean "something is happening" | `CONSOLE-NOTES.md` §17's closing line: *"`live` in a picker row means 'a process is running', not 'something is happening' — and the design's status vocabulary has no word for that difference"* · `TeamSummary.live` = `leadAlive \|\| within IDLE_GRACE_MS of lastActivityAt`, over the mtimes of `config.json` and `inboxes/*.json` | **No. The picker's copy stays exactly as it is.** Ruling 25 makes the *agent's* status honest and does not touch the team's. | They are two different questions off two different sources, and only the second one got fixed. The picker asks *is this team's process alive, were its files touched*; ruling 25 asks *has this agent written anything*. Deriving the picker's state from agent statuses instead would mean folding every listed team's event log, which the teams listing refuses on a measurement already recorded in `domain.ts`: folding one team's log to report on it cost **48× the whole listing**. Buying a better word for one row at 48× the cost of the entire dropdown is the wrong trade, and a half-applied version — right for the current team, mtime-based for the rest — would be worse than the honest one it replaced. §17's observation stands as a note to the design, not as work. |
| 27 | `blocked` | Team panel 7's dollar row — "spend attributable to re-reading inboxes" | `USAGE-DASHBOARD.md` team panel 7: messages delivered · **spend attributable to re-reading inboxes** · agent time idle, *"labelled as attribution, not measurement"* · `USAGE-STATE.md` §6: **not derivable** — "no token is attributable to a message read" — draw the other two and drop the dollar row · task #15 lists it as buildable if captioned | **§6 upheld: the dollar row is dropped.** The panel ships with messages delivered and agent time idle, both countable, and a line in place saying the third figure is not derivable rather than leaving a gap. The caption `attribution, not measurement` does not license it. | The usage ledger reports per-turn totals and never per-content-block attribution, so nothing on disk says which tokens of a turn were the inbox. Deriving it means approximating the message text's token length, multiplying by a guessed number of turns it stayed in context, and applying the cache-read rate — three estimates chained and served with a currency symbol, which is the exact failure ruling 21 refuses in workflow mode. The same question may not get two answers in two modes. **Why panel 8's estimate survives and this one does not**, since they look alike: panel 8 is an explicit *counterfactual* — what this work would have cost run serially, a thing that never happened — and a caption can honestly mark a counterfactual as one. Panel 7's row purports to measure something that **did** happen and simply was not measured. A caption changes how a number is read; it cannot turn an unmeasured actual into a measured one. |
| 28 | `reconcile` | The closed theme row's label: the README's worked example, or `movie-themes.json` | rev 5 README's merged-dropdown example: `Inception · Limbo grey` · `movie-themes.json` v3: Inception's `palette.label` is `steel & kick` | **The JSON is canonical. The closed row renders `<film> · <palette.label>`.** | The README's own movie-themes section instructs reading the database rather than transcribing from the prose, and the CHANGELOG says outright that where the two diverge the JSON wins. `Limbo grey` is a worked example written beside the data, not a third source. |
| 29 | `deviation` | The repro sprite's rest shirt is `e` (the failure rose), so its film garment can never render | The five-glyph recolour rule: a look repaints `s\|S\|a\|b\|h` only · `ROLE_SPRITES.repro` rows 9-11 are `e` · `SPRITE_COLORS` maps `e: var(--fail)` · v3 `looks` declares a `garment` for the repro slot in all ten films · film implemented the spec literally and did not improvise | **An oversight in the sprite, not a deliberate choice. Repro's three shirt rows change from `e` to `a`**, matching every other role; `e`/`var(--fail)` stays strictly the failed-status treatment. Fix the sprite data, **not** the recolour rule — a special case mapping `e` to the garment slot would only help while a film look is active and would leave the rose in place on the six system themes, where the rule is broken too. | Positional proof, which settles it without appeal to intent: rows 9-11 are the shirt on every role, and `perf`, `tests` and `architect` all draw them as `..aaaaaaaa..` / `.aaaaaaaaaa.` / `.aa.aaaa.aa.`. Repro draws the **identical geometry** as `..eeeeeeee..` / `.eeeeeeeeee.` / `.ee.eeee.ee.`. It is the garment glyph wearing a different letter. Then three consequences, any one of which is enough: standing rule 2 says no role borrows a semantic token, and this is a role identity painted in the failure colour at rest; ten films each declare a repro garment that under the current rule can never be painted, which is data with no consumer; and — the practical one — a repro agent that **actually fails** is indistinguishable from one at rest, because both are rose. A status colour that is always on cannot signal status, and it is switched permanently on for the role most likely to need it. **Two consequences to expect rather than be surprised by.** This is visible with NO film selected: `a` resolves to `--color-accent-400`, so the repro portrait's shirt goes from rose to the theme accent on the default console, matching the other five roles — that is the ruling working, not a side effect, since standing rule 2 is about the token and applies on every theme. And the test pinning the current pixel (`bakes the failure rose into the repro shirt`) inverts; it should be rewritten to assert the RULE — that no role sprite paints its rest state in a semantic glyph — which is the thing worth protecting and would have caught this when the sprite was written. Measured by film across the data: five of six roles paint the shirt in `a`, repro alone in `e`, and all ten films declare a repro garment and shade that can never render — plainly considered per-character values, from Mr. Blonde's near-black `#1f1f1f` to Luke's near-white `#e2ded2`, which is exactly the spread the silhouette lift exists to handle. Rendered, six films' repro characters are identical below the neck. |
| 30 | `reconcile` | The third note under the theme dropdown, whose copy the design never wrote | README:133 spells two of three states — no film: *"Agents keep their real names."*; film palette off: *"Names and portrait colours only; the ground stays on Nocturne."*; film palette on: **"the full sentence"**, never expanded anywhere in README, CHANGELOG or `movie-themes.json` · the prototype is rev 3b and predates the merged picker | **`Names, portrait colours and the ground all come from <film>.`** — film's proposal, upheld as written. | The two captions are read as a pair, one after the other, by an operator flipping the switch — so the second must name the same thing the first withheld, in the same words. State 2 already says *the ground*; state 3 grants *the ground*. Echoing the FILM group's label instead (*"…and the palette all come from…"*) reads better in isolation and worse in place, because calling one thing "the ground" and then "the palette" one line apart makes the operator work out whether two nouns are one thing. State 2's included-then-excluded shape also fixes the construction: the same three nouns, with nothing taken away. |
| 31 | `blocked` | `agentsMeasured`: draw a partial workflow total with its coverage, or em-dash it like a team total | #24's contract carries `agentsMeasured` so a view "can say what the figure is still missing rather than presenting a partial total as the whole" · the standing team rule (`sumSplit`): an absent contributor poisons the sum, so a team tile em-dashes when any one agent's split is unrecorded · on a live 20-agent run with 6 measured the two give opposite answers | **Split on `run.live`, which is the thing that actually differs.** On a **live** run: draw the figure with its coverage beside it — `$0.42 · 6 of 20 agents measured`. On a **finished** run: the team rule governs — any unmeasured agent em-dashes the total. | The team rule exists because an unrecorded split is a hole of **unknown size**: the sum has no bounded relationship to the truth, and it reads as whole. Neither half of that holds on a live run. An agent that has not started has contributed exactly **zero** so far, not an unknown, so the partial total is the true spend *to date* rather than an understatement — and the reader already knows a running thing is unfinished. The team tile also has no meaningful denominator to offer (the count of pre-split records means nothing to an operator), while a run has an obvious one, which is why a coverage label is legible here and would be noise there. Then the case flips at termination, and this is the half worth being strict about: once a run has ended, nothing more is coming, an unmeasured agent is a permanent hole of unknown size, and `18 of 20 measured` would be reporting a **defect** in the reading as though it were a status of the run. That is exactly the team rule's case, so it takes the team rule's answer. Practical bonus of splitting here rather than on the kind of absence: no flicker. Deciding per-agent (started-but-unread vs not-started) would blink the tile between a number and an em-dash every time an agent spawns. |

## The four that need more than a table cell

**1 — what the quiet register covers.** It is small, quiet *metadata text*: column-header rows,
footer notes, meta and hint lines, key legends, empty-state copy, timestamps, delivery receipts,
read-only markings, the composer's `⏎` glyph. All of it `--color-neutral-600`.

**The size is a floor, not a target.** 10px is the smallest this register may be drawn, and
anything below it — the 9.5px the contrast rule was written against — comes up to 10. Text
already at 10.5 or 11px stays there: the prototype draws this register at 10.5px in the wall,
the rail and the task table, and shrinking it would cost legibility in the name of a rule whose
entire purpose is legibility. Match the neighbours in the same pane rather than one global
number — the wall's read-only marking sits with the current-tool line at 10.5px, the rail's sits
in the composer's slot at 11px, the drawer's meta lines are 10px.

It does **not** cover `--color-neutral-700` used as a status or state colour — `done` in the
picker, `wait` in the workflow run view, `StopButton`'s rest state — or as a border, shadow or
gutter token. Those carry meaning from their own palette and a ramp step is per-theme already.
Recolouring them would be the mistake standing rule 2 warns about, in the other direction.

**2 — the threshold formula.** Two stages of one warning, both re-based on the displayed percent:

- `!` fires at `tokens / contextLimit >= 0.75`.
- the note fires at `tokens >= threshold + (compactAt − threshold) / 2` — halfway from the
  threshold to the trigger — and still reports the distance to `compactAt`, in tokens.

The note keeps its current shape and its own header row; only the base of the first stage moves.
No model in `catalog.json` inverts the two (`compactAt` is 83.5% of a 200k window and 96.7% of a
1M one), so the threshold always sits below the trigger.

**10 — where this lands.** `STATE` 118 / `DEPENDS ON` 76 applies to the 4a tasks view *and* the
3b coordination view, since the stepper is specified for both.

**14 — the measured part.** The picker row's second line is `name · branch … agents · state`,
and only `branch` can shrink — `name`, `agents` and `state` are all `nowrap` with no ellipsis.
At 10.5px JetBrains Mono, `session-8f2a1c` is ~88px, a 24-character branch ~151px, `4 agents`
~48px and `ended 41m ago` ~78px; with 24px of padding, a 19px glyph column and three 8px gaps
that is ~452px of content before the goal line is considered. 432px clips the branch on a
realistic repo; 520px leaves the slack the reconcile's two-line row needs.

## The deviations, with what they cost

Four rows depart from the canvas. Each is listed with the artboard it departs from, so the next
person can see what was traded rather than only the argument for trading it.

**4 — transcript line gap.** Canvas draws fixed leading; we made it density-driven. The config
control has to change the render or it names nothing, and the canvas's two figures are each a
snapshot of one density setting. Costs nothing visible at `default`.

**23 — bare windows in the picker.** *Superseded 2026-09-03.* The carve-out survived only
because a bare window had nowhere to go; it draws its own stream now, so the filter and its
`reveal` hatch are gone. The `✕` stays — an operator action, not a rule.

**24 — the solo switcher, and `stream` as a view. Reopened and resolved
2026-09-03.** Ruling 24 read the README's prose (`stream · trace · usage`),
decided `stream` was "a prop, not a view", and shipped four pills. The canvas
builds the switcher itself:

```js
saViews: [['stream', 'stream'], ['trace', 'trace']]
```

Two pills. `tasks` and `usage` were never in it. Both halves are now fixed: the
list is `stream · trace` (and `stream` alone with no subagents), and the "prop"
the ruling promised — full width — is delivered, so a roster of one fills the
frame instead of sitting in a 366px column. The Task row and its drawer needed
no work; they were already built in `TranscriptFeed`.

**The cost is real and was 24's own argument:** 9 of the 10 non-empty task lists
measured belonged to sessions rather than teams, and a solo session can no
longer reach its list from the switcher. If that bites, the one-line revert is
in `soloViews`.

**29 — the repro sprite's shirt.** Corrects an oversight in the design's own sprite data rather
than a design choice. Uncontroversial; listed for completeness.

## Known-good, and why — re-check these, do not assume them

Previously headed "do not fix these back", which stopped them being re-examined at all. They are
settled, not sealed: the reasoning is here so a future change can argue with it.

- **No mailbox pane in the tasks view** (or in the `3b` coordination frame). The task list fills
  the frame; messages live in comms, which gained the everyone room to hold the merged feed the
  mailbox pane used to carry.
- **The `7a` lifecycle is untouched.** `stop watching` is view-local, writes nothing to
  `~/.claude`, and needs no change to the idle reaper, `IDLE_GRACE_MS`, or the `SessionEnd` fast
  path. The reaper reads only the filesystem and keeps the process alive while any team has two
  or more members. The CHANGELOG's own correction says the earlier claim of a lifecycle change
  was wrong. "The server stays up" is a description, not a mandate — do not turn it into a
  process that never dies.
- Rulings 4, 5, 6, 7b, 7c, 8, 9 and 13: the app already matches. Listed so nobody reverts them
  to the README's older figure.

## Closed against the canvas, 2026-09-03

Found by rendering the canvas, reading its own template variables, and diffing both against the
running app. Screenshots in `docs/session-type-screens/` (`design-*` are the artboards,
`final-*` the app).

| Artboard | What it asked for | State |
|---|---|---|
| all | `OCTO` wordmark, then an outlined kind badge in the trigger before the goal | **done** — `KIND` table transcribed, colours and 9.5px included |
| all | every picker row leads with its kind pill, 9px | **done** — the activity cell stopped repeating the kind as a result |
| all | the goal `max-width:146px` | **done** — was a fixed `width`; ruling 14 said "capped" and the canvas agrees |
| `8a` | `saViews` — two pills, `stream · trace` | **done** — supersedes ruling 24's four |
| `8a` | bar reads `turn N of M` then `working · 4m 08s` | **done** — turn count from the full record set, elapsed from the CURRENT turn |
| `8a` | strip: subagents · max depth · tokens-in · shown-to-parent · spend + ratio note | **already built** — cosmetics aligned (gap 26, baseline, 15px, accent on shown-to-parent) |
| `8b` | full-width stream, Task row expanding into a drawer, own footer | **done** — the drawer already existed; only the width was missing |
| `4a` | seven pills, `TaskList — N tasks, M blocked` above the composer | **done** |
| `4a` | current-tool line under each non-lead column | **already built** — it clears on `PostToolUse`, so it only shows mid-tool |
| `6a` | five pills `run · agents · script · journal · usage` | **already correct** |
| `6a` | work-item × phase grid; bar reads task · budget bar · budget text · elapsed | **`blocked`** — phases and budget reach disk only in the terminal snapshot |

Two things the canvas does not answer, both gap-filled and marked as such in the code:

- **A window with nothing in it.** The canvas's `SESSIONS` fixture knows three kinds —
  `teammates`, `subagents`, `workflow`. There is no artboard for a bare session, so `solo`
  borrows the quietest colour pair and offers `stream` alone, by the same
  "a view is offered when its subject exists" rule.
- **Workflow step names.** `workflow-subagent` metas carry no name, so the agents view lists
  raw ids. Needs the parent transcript's `Workflow` tool_use joined on the `wf_` id.
